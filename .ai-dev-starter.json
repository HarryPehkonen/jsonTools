{
  "kit": "AI-DEV-STARTER",
  "url": "https://github.com/HarryPehkonen/AI-DEV-STARTER",
  "revision": "f9c33005d880df095d7a5ab300d1ec5349c59aea",
  "recorded_at": "2026-09-20",
  "record_kind": "retroactive",
  "record_note": "Wired by hand on 2026-09-19 (commit 30897dc, 'build: gate every commit with tools/ci.sh + .githooks (format, lint, tests, invariants)'), before the kit existed as a git repo — so `revision` is the kit's FIRST PUBLISHED commit, not the exact copy point; do not read it as the tree this repo was copied from. Five of the six artifacts here are deliberately adapted for jsonTools' 12-stage gate, and each reason is below as well as in the header of tools/ci.sh: the kit's C++ template ships 10 stages, this repo adds cli/fuzz/wire, drops tsan, and builds offline against the sibling JSOM checkout. Two PLUNK-IN step-5 artifacts are absent and are deliberately NOT entries: INCIDENTS.md was never copied (git log --all -- INCIDENTS.md is empty, and no equivalent file exists), so jsonTools records its deviations in the headers of the three copied scripts instead; and CLAUDE.md is this repo's own contract, not a copy of the template (see its entry). The kit's Deno and Python artifacts (templates/deno/gate.sh, templates/python/gate.sh, the kit's own .gitignore) never applied to a C++ repo and are not listed. Rule that keeps this file true: any commit that edits a copied artifact must update that artifact's repo_sha256 in the same commit. **2026-09-20, card t_0cc793fb:** the `kitprobes` stage and `tools/kit-probes/tidy-baseline.sh` were taken from the kit at the revision above — a fix that must propagate ships a probe, and this copy now runs it in its own gate. `revision` moved to that commit because these entries come from it; the earlier entries are still the fb387f2 wiring, and the kit files behind them have not changed between the two commits (the ones that did — `templates/cpp/ci.sh`, `templates/hooks/pre-push` — were ported in this same commit). **2026-09-20, later the same day (still card t_0cc793fb):** `revision` moved to 951608c2675a… for two more artifacts from the kit — the gate's dispatch guards (a stage that dies or returns non-zero without a verdict can no longer leave a green run; measured on this gate's own `local -a sources`, see INCIDENTS.md) and that fix's own probe `tools/kit-probes/gate-stage-guards.sh`. Between the previous revision and this one the kit changed only `templates/cpp/ci.sh`, `INCIDENTS.md`, `DESIGN-NOTES.md` and the new probe; the entries behind the other files are unchanged by it. **2026-09-20, card t_772eabd9:** tools/ci.sh gained CI_BUILD_TYPE (Release, passed to every configure call) and .ci.env.example documents it — the build type this consumer used to inherit by force from JSOM's CMakeLists, which the JSOM-side guard removed; both files are re-hashed here. **2026-09-20, card t_fb62d8fa:** the record gained the kit's per-fix lists (docs/KIT-REVISION-CONVENTION.md, 'Exact file shape'), so what this copy has and has not taken from the kit is machine-readable instead of prose. `adopted_fixes` names the two probes this copy carries, with the kit revision each fix came from -- and that claim is now CHECKED, not just asserted: the record checker runs each probe against tools/ci.sh, and a claim whose probe is absent or fails is a record failure. `declined_fixes` records the kit's `optimized-build-stage` (probe `probes/optimized-stage.sh`, added by the kit at d531a110) as NOT TAKEN: run against this gate it FAILS (measured 0 ok, 7 failed at this HEAD), and whether this fork takes that stage is parked with Harri. Saying so in the record is the point -- the alternative, moving `revision` to d531a110, would claim this copy was wired from artifacts it never took. **`revision` and every `kit_sha256` are UNCHANGED by this commit**: nothing here came from the kit, and being behind the kit is not a failure -- the check that called it one was measuring whether the kit had moved. **2026-09-20, card t_0a9a0018:** the kit's GIT_INDEX_FILE fix was ported — kit f9c3300 — as one line in tools/ci.sh's prologue plus the kit's own probe at tools/kit-probes/git-index-file.sh, and `revision` moved to f9c3300 because this files[] set now takes that probe from it. Nothing else this copy carries changed between the two revisions, so every other entry's `kit_sha256` is untouched; the one that did move is tools/ci.sh's, because the kit's templates/cpp/ci.sh is where the fix landed. This repo takes it although its own gate reads only this checkout: the shape is `CI_JSOM_DIR` empty (the knob's own comment: `empty = let CMake FetchContent JSOM`), where cmake's update step runs `git status` inside the JSOM clone it just made — the measured failure (Computo, cards t_9541aa62 -> t_0a9a0018) — and this copy now also carries probes that run git in throwaway repositories by design. `adopted_fixes` claims the fix and the record checker RUNS the probe against this repo's gate.",
  "adopted_fixes": [
    {
      "name": "tidy-baseline",
      "from_revision": "a6ad2ea6951c66eb5b7c2eea8757ab8d3311174e",
      "probe": "probes/tidy-baseline.sh",
      "adopted_in": "c81c9b5"
    },
    {
      "name": "gate-stage-guards",
      "from_revision": "951608c2675a2f5c2d0b59d9c19c632d23a043af",
      "probe": "probes/gate-stage-guards.sh",
      "adopted_in": "06cb86c"
    }
  ],
  "declined_fixes": [
    {
      "name": "optimized-build-stage",
      "from_revision": "d531a110184125ed108c3eabc36834bb803404bb",
      "probe": "probes/optimized-stage.sh",
      "why": "not taken: parked with Harri (2026-09-20). This gate states CI_BUILD_TYPE=Release everywhere, so the kit's stage (a second configuration at Release by default) would build the same configuration twice; the configuration this gate never checks is Debug, and that port is the other way round. Measured: the kit's probe FAILS against tools/ci.sh (0 ok, 7 failed)."
    }
  ],
  "files": [
    {
      "repo_path": "tools/ci.sh",
      "kit_path": "templates/cpp/ci.sh",
      "repo_sha256": "5e594f8acf4603738a2c4b42cd20e9736fbccd3a550c8dbbff23bbc6bda1da69",
      "kit_sha256": "166e0042aa81ce24874c994a756e0c62d435da31398cd8d79d81d7600e554191",
      "adapted": true,
      "adapted_why": "The one copy that is a superset rather than an edit: stages ADDED are cli (spawns each jt_* binary), fuzz (libFuzzer smoke with CI_FUZZ_BUILD_DIR/CI_FUZZ_SECONDS) and wire (tools/check_wiring.py); stage tsan is DROPPED (no threads in this repo yet); CI_JSOM_DIR is added so the gate builds against the sibling ../JSOM checkout instead of FetchContent (offline, and every run prints the JSOM revision it used); CI_TEST_CMD is unused because there is exactly one gtest binary. Its header lists every deviation so nobody has to diff it against the kit. The tidy-baseline comparison is the kit's CURRENT tidy_key form (ported in 7ecc1d2), so a sync should diff this file, never overwrite it. Since t_772eabd9 (2026-09-20) it also defines CI_BUILD_TYPE (default Release) and passes it to every configure call — the configuration this tree previously got by FORCE from JSOM's CMakeLists while JSOM entered as a subdirectory. **2026-09-20, card t_0a9a0018:** the prologue gained `unset GIT_INDEX_FILE` — the kit's own line now (kit f9c3300), with the mechanism and the measurement in the comment above it: git exports its TEMPORARY index to the pre-commit hook for a `git commit -- <path>`, and any `git` command this gate runs inside ANOTHER repository then reads this repo's index entries against that repository's object store and dies on the first blob it does not have. This repo takes it although its own gate reads only this checkout: the shape is `CI_JSOM_DIR` empty (the knob's own comment: `empty = let CMake FetchContent JSOM`), where cmake's update step runs `git status` inside the JSOM clone it just made — the measured failure (Computo, cards t_9541aa62 -> t_0a9a0018) — and this copy now also carries probes that run git in throwaway repositories by design."
    },
    {
      "repo_path": ".ci.env.example",
      "kit_path": "templates/cpp/.ci.env.example",
      "repo_sha256": "65964576c8e065b85d4ee59c11ea3d9e886b3030735ba7b27c1bdfd4b177e41d",
      "kit_sha256": "eee5264aaa1b120fcd76685879559aae15bbf0fe39c45cfb0c4726d026709556",
      "adapted": true,
      "adapted_why": "Documents THIS repo's gate: the 12-stage CI_DEFAULT_STAGES, CI_JSOM_DIR (sibling checkout or FetchContent when unset), CI_FUZZ_BUILD_DIR/CI_FUZZ_SECONDS, the absence of CI_TEST_CMD, and CI_BUILD_TYPE (default Release, passed to every configure call since t_772eabd9, so the gate asks for the configuration it used to inherit) — and drops the kit's sections that have no counterpart here (CI_TSAN_BUILD_DIR, CI_SOURCE_GLOBS, CI_CMAKE_FLAGS, CI_VERSION_HEADER, CI_ASAN_TEST_CMD). The clang-tidy baseline comment is the kit's current --write-tidy-baseline text, not the old hand-capture recipe (ported in 7ecc1d2)."
    },
    {
      "repo_path": ".githooks/pre-commit",
      "kit_path": "templates/hooks/pre-commit",
      "repo_sha256": "3ad79e469edf955746ee2c69c60bb319f7b48da8bdc0e2a8eaa92a6e835164f0",
      "kit_sha256": "e304ddcb48e216029c8e77402d927695dae4895532f6bf12d1ecb54604b652d8",
      "adapted": true,
      "adapted_why": "Deviation declared in its own header: jsonTools' fast tier is `tree build tests`, not the kit's `build tests`, so 'I wrote the new file and forgot to git add it' fails the commit instead of the push; the kit's fallback branch for Deno/Python repos (scripts/gate.sh) was deleted, since this is a C++-only repo."
    },
    {
      "repo_path": ".githooks/pre-push",
      "kit_path": "templates/hooks/pre-push",
      "repo_sha256": "346d49d34c967e660e4a1fa35f9dc4dafe04faf76fbbb5b273393acbd79df939",
      "kit_sha256": "f3ee3909b042ba74728f09b8c68aa2a82f23fe212ebea1f79aae89f5cef9ca07",
      "adapted": true,
      "adapted_why": "Deviation declared in its own header: this hook passes NO stage list (`tools/ci.sh --require-clean`), because CI_DEFAULT_STAGES inside tools/ci.sh is the single definition of the gate — a list here would be a second copy of it and would also drop this repo's cli/fuzz/wire stages; the kit's Deno/Python branch was deleted. Its header was rewritten in c78a26e (comment-only) to stop claiming to be the repo's only hook."
    },
    {
      "repo_path": "CLAUDE.md",
      "kit_path": "templates/CLAUDE.md.template",
      "repo_sha256": "b926b1d8e1c4cd10453ee0b1d269533583f04d8137cfcc4298c0089152cef876",
      "kit_sha256": "615ad65a51eb5b70a59882d261eeacc1510cbd7c8cd59d730bba945ad19fa3da",
      "adapted": true,
      "adapted_why": "Not a copy, and the direction matters: this contract predates the kit (created 2026-09-08 in 001caf9, two weeks before the kit's template) and the kit was distilled FROM contracts like it — DESIGN-NOTES D3 says the kit's artifact-identity stage REPLACES jsonTools' wire stage. Copying the placeholder template over this file would delete every project-specific command in it, so a sync must never do that. The one kit-shaped change is the 'Local CI' paragraph that now records the two-hook split (44c3f6c, 2026-09-20, written with Harri's explicit approval because CLAUDE.md is a protected agent-instruction file and the earlier headless write was refused; that draft is kept at ../gate-evidence/jsonTools/CLAUDE.md-hooks-section.patch, path relative to this repo root). The contract is LIVE at this path — no staging path remains."
    },
    {
      "repo_path": "tools/kit-probes/tidy-baseline.sh",
      "kit_path": "probes/tidy-baseline.sh",
      "repo_sha256": "efd9bbca108bb7bd1e527d95d09546c1b3bceca451054367b2fed6f92bfc6379",
      "kit_sha256": "efd9bbca108bb7bd1e527d95d09546c1b3bceca451054367b2fed6f92bfc6379",
      "adapted": false
    },
    {
      "repo_path": "tools/kit-probes/gate-stage-guards.sh",
      "kit_path": "probes/gate-stage-guards.sh",
      "repo_sha256": "0a9f752c5081d6cc61b0f478ebee356d1358c7aca9562643411249901178be86",
      "kit_sha256": "0a9f752c5081d6cc61b0f478ebee356d1358c7aca9562643411249901178be86",
      "adapted": false
    },
    {
      "repo_path": "tools/kit-probes/git-index-file.sh",
      "kit_path": "probes/git-index-file.sh",
      "repo_sha256": "6818e5d5b049edc250dc5b7cd1e620917cf80afef0bbade83b8cbb690ddc27e6",
      "kit_sha256": "6818e5d5b049edc250dc5b7cd1e620917cf80afef0bbade83b8cbb690ddc27e6",
      "adapted": false
    }
  ]
}
